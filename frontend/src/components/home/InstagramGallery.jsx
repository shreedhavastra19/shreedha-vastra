

// Static placeholder gallery — replace image URLs with real Instagram
// post images (or wire up the Instagram Graph API) once available.
// Using picsum.photos as reliable neutral placeholders for now.


const InstagramGallery = () => (
  <section className="container-custom py-16 text-center">
    <h2 className="section-heading mb-2">Follow Our Journey</h2>
    <p className="text-gold mb-8 font-medium">@shreedha_vastra_</p>
  </section>
);

export default InstagramGallery;
